#include "lexer.h"
#include "parser.h"
#include <string>

#include <fstream>

#define ltests "Tests\lexer"
#define lcnt 4
#define ptests "Tests\parser"

void lexer_tests(){
	for (int i = 0; i < lcnt; i++){
		FILE *fin, *fout;
		
	}
}